import { Typography } from "@mui/material";
import React from "react";
import DefaultLayout from "../../layouts/DefaultLayout";

export default () => {
  return (
    <DefaultLayout>
      <Typography variant="body1">디지털 / 가전제품</Typography>
    </DefaultLayout>
  );
};
